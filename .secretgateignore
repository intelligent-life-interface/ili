# Diese Datei zitiert FP-Strings in Kommentaren -> selbst ausgenommen
.secretgateignore
# 26.08.2026: eigenes Ignore-File für privacy-scanner, zitiert FP-Beispiele
# (Klarnamen/IPs als Doku-Muster) -> gleicher Grund wie oben, selbst ausgenommen
.privacyignore
# Belegte False-Positives (strict-Modus):
# host.containers.internal = Standard-Podman-Alias, keine private Adresse
constants.py
docs/ERSTELLUNG.md
html/_api-locations.conf
mcp/Containerfile
mcp/server-docker-registry.yaml
mcp/server.yaml
# f-string `user = f"..."` bzw. Doku-User "ili" (öffentlicher Produktname)
project_creator.py
docs/PROJECT-TERMINAL.md
# Beispiel-IPs (192.168.1.x, 10.0.0.x) als bewusste Test-Fixtures des Sanitizers
tests/test_github_issue_service.py
tests/test_report_sanitizer.py
# 07.09.2026: Docker-Host-Format-Validierung — 192.168.1.10:2375/10.0.0.5:2376
# sind erfundene Beispiel-Hosts zum Testen von validate()/env_overrides(),
# keine echte LAN-Adresse
tests/test_docker_service.py
# minified JS: `c.local`-Artefakte, kein Hostname
html/github-status.html
# Doku-Beispiel-IP-Range (192.168.1.x = Beispiel, nicht das echte LAN)
README.md
QUICKSTART.md
.env.example
# env.local / settings.local = Dateinamen-Artefakte, keine mDNS-Hostnames
.gitignore
app/api/claude_theme.py
# zitiert Klarname-Suchbegriffe nur als grep-Suchmuster in Punkt 2 der Checkliste
docs/PUBLISH-CHECKLISTE.md
# 26.08.2026 (Release 0.1.12): MCP-Server-Dateien — nur oeffentliche Repo-URL
# (github.com/Toa1984/ili-public), Standard-Docker-Alias host.containers.internal
# und Loopback 127.0.0.1; keine privaten Adressen/Namen (geprueft per grep)
mcp/Containerfile
mcp/server.yaml
mcp/server-docker-registry.yaml

# 19.09.2026 (Release 0.1.22): die mitgelieferten Claude-Skills. Sie beschreiben
# Konventionen und nennen dabei Loopback (127.0.0.1), das Netz-Praefix 192.168.
# ohne konkrete Adresse und den Standard-Alias host.containers.internal — in
# net-check.sh sogar als Suchmuster der Pruefung selbst. Alle Echtwerte sind
# vom Skill-Export durch Platzhalter ersetzt (<DOMAIN>, <SERVER_IP>), belegt im
# EXPORT-MANIFEST: 43 Ersetzungen. Einzeln geprueft: keine echte Adresse, keine
# Domain, kein Home-Pfad, kein Name.
deploy/terminal/skills/architektur-review/SKILL.md
deploy/terminal/skills/container-scaffold/SKILL.md
deploy/terminal/skills/netzwerk-konventionen/SKILL.md
deploy/terminal/skills/netzwerk-konventionen/net-check.sh
deploy/terminal/skills/security-konventionen/SKILL.md

# 20.09.2026: Testdatei des SSH-Assistenten. Zeile 17 haelt einen Dummy-Schluessel
#   ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIFakeFakeFake...Fake0 test@host
# — der Base64-Teil besteht aus dem Wort "Fake". Ohne so einen Fixture laesst
# sich die Schluessel-Pruefung nicht testen. Kein echtes Material.
tests/test_ssh_setup.py
